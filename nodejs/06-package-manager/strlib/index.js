/**
 * Find index of a given string
 * @param {string} src 
 * @param {string} value 
 * @returns {number}
 */
export function findIndexOf(src, value) {
    return src.indexOf(value);
}

/**
 * 
 * @param {string} src 
 * @returns {number}
 */
export function sizeOf(src) {
    return src.length;
}
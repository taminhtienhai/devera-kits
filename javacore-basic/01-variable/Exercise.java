
public class Exercise {
    public static void main(String[] args) {
        fix_bellow_error();
    }

    /// Mở comment và khiến cho đoạn code sau chạy đúng
    public static void fix_bellow_error() {
        // final int a = "";
        // final String b = 1;

        // a += 10;

        //// Không được sửa dòng này
        // assert a == 10;
    }

    /// Khai báo 2 biến `a` và `b` có kiểu dữ liệu lần lươt là int và Integer
    /// và tính tổng của chúng vào biến `c`
    /// Trong trường hợp này kiểu dữ liệu của `c` là gì? Hãy đưa ra câu trả lời trước khi thực hiện code.
    public static void declare_variable() {
        // assert a + b == c;
    }
}
